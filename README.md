# Technical Support Bot using RAG

A Retrieval-Augmented Generation (RAG) based Technical Support Assistant that answers questions from the Upwork API documentation.

The application extracts content from a PDF, stores document embeddings in ChromaDB, retrieves the most relevant documentation using semantic search and generates answers using an LLM.

---

# Tech Stack

- Python
- Streamlit
